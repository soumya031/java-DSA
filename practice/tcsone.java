package practice;

import java.util.*;

public class tcsone {
    public int subarraysum(int[] arr){
        int[] sumMap = {};
        int curSum = 0;
        for(int i = 0; i < n; i++){
            curSum += arr[i];
            if(curSum == target){
                System.err.println(arr[i-1]);
            }
            if(curSum - target)

        }
    }
}
